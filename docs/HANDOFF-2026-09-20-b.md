# babyshower.com.py: hand-off part 2 (2026-09-20, after Batches 5 and 6)

Repo: `C:\Claude 1\babyshower`, branch `master`. Written by the Claude Code manager session (Sonnet 5). Supersedes nothing: read `docs/HANDOFF-2026-09-20.md` first for Batches 0 to 4 and the environment traps; this file adds what happened since.

## 1. What was done since the first hand-off

| Commit | What | Worker |
|---|---|---|
| `612cc32` | Design references: `docs/design-mix-hero.dc.html`, `docs/design-mix-colors.dc.html`, the approved mixed design `docs/design-mix-home/` (four boards, `canvas.json`, `gen.mjs` generator that builds the boards from `content.mjs`), and `preview-server.mjs` now accepts the port as an argument | manager |
| `ce57c86` | **Batch 5**: home rebuilt from the approved mix: full-bleed photo hero with scrim and overlapping trust ribbon, tinted-header pricing cards (Estrella raised and first on mobile, coral button only on Estrella), blush calculator panel, band photo, numbered step cards, theme photo cards with swatch fallback, include/exclude lists (replace the comparison table on the home only), sky zones panel with surcharge amounts, one new FAQ answer, two new H2s, "(Sueno)" annotation removed from Combo Sueno | Codex gpt-6-astra low, session `01a0bedf-f8f7-71d0-9c72-6b7b0e50feb2` (2 turns) |
| `72d22df` | **Batch 6**: inner pages moved to the same components (offer cards, theme cards, numbered steps), grids fixed at 640 to 959 px, 44 px tap targets, scroll cue and sticky first column on the comparison table, spacing, lighter smooth hero scrim, dead CSS removed (9029 to 7703 bytes brotli), `css-audit.mjs` added | Codex gpt-6-astra low, session `01a0beed-6cbd-7582-b3e8-b1fc46809333` (3 turns) |

The approved design lives in the Claude Design artifact https://claude.ai/artifact/Jhtg619t4DUojuRkp5cYNC (four boards: desktop 1/2, 2/2, mobile 1/2, 2/2). Model and effort of every Codex turn were read from the session `turn_context`, not from the worker's chat.

## 2. Verified by running it

- `node build-site.mjs` and `node verify.mjs --final` pass; `node verify.mjs --final --http http://127.0.0.1:4185 --zip dist/babyshower-2026-09-20.zip` exits 0; the zip was rebuilt from `72d22df` (105 entries, 32 routes).
- Screenshots (Playwright + installed Chrome) of the home at 375, 768, 1024, 1440 and of the fixed inner pages (theme, reveal, combos, zone, anito) at 375 and 768; pixel diff before and after the CSS trim identical on 6 of 7 pages (the seventh differs only by the tap-target fix).
- Tap-target script (`docs/qa-tools/tap-targets.mjs`): remaining flags are only narrow breadcrumb widths and the 24 px checkbox inside a 44 px label.
- Lighthouse mobile home: best run 99 / 100 / 100 / 100. **Important: on this laptop the performance score is bimodal, 89 to 91 (FCP 2.6 s) or 98 to 99 (FCP 1.5 s), for identical files.** The committed Batch 5 files scored both. Compare only runs made back to back in the same window, never against an old number. Accessibility, best practices and SEO were 100 every time.

## 3. NOT verified

- PHP was never executed (not installed here). Hosted form test is Anton's.
- Real-host compression (`Content-Encoding` on the live server) and real phone timing.
- Three WhatsApp buttons show at once around 768 px (header button, floating button, mobile bar): left as is, cosmetic.
- Small known leftovers, all cosmetic: `/zonas/` zone paragraphs are long; the `/contacto/` date field shows the browser locale format; breadcrumb links are 44 px tall but only about 35 px wide; combo calculator radio labels have generous left spacing.
- The one unrelated untracked pair, `plan/prompts/DESIGN-FINAL-4.txt` and `DESIGN-FINAL-5.txt`, is still uncommitted (plan/ is off limits for the manager).

## 4. Decisions status (D1 to D11 from the first hand-off)

- D1 badge Combo completo and Todo listo: kept, still Anton's call.
- D2 "(Sueno)" annotation: **removed** in Batch 5 (Anton saw it removed in the design and approved the design); one-line revert in `content.mjs` PACKAGES[2].description if he objects.
- D3 hide the proposal card on mobile: **resolved** by the new hero (the card no longer exists).
- D4 calculator result caption: open (kept the existing wording).
- D5 og.jpg with its own title text: open.
- D6 Q21 notification email: open. D7 which design governs: the mixed design (`docs/design-mix-home/`), Anton to confirm. D8 images: open. D9 Capiata and Mariano Roque Alonso pages: open, recommendation is hub only. D10 display table for the mesa dulce: open. D11 rule for `PRICES.effectiveDate`: open.
- New copy from Batch 5 that Anton should read (REVISAR ANTON in `docs/log/build.md`): FAQ item "Por que los precios son estimados", the home H2s "Cuatro pasos, sin vueltas" and "Lo que conviene saber antes de escribir".

## 5. Things only Anton can do

1. Answer D1, D4, D5, D6, D8, D9, D10, D11 and confirm D7 (see section 4). Two minutes each, most can be a one-word answer.
2. **Notification email (Q21)**: choose the address that receives leads, put it as `BABYSHOWER_LEAD_EMAIL` in the private `vendercrm-config.babyshower.php` above `public_html` on the host, and decide the public `SITE.email` (may stay empty). Until then every real form submission shows the safe failure message and no lead is stored.
3. **Hosted PHP test** (after uploading the zip, see `docs/DEPLOY.md`): submit the form with `0981 234-567`; submit once with JavaScript disabled and check that a `BS-yyyymmdd-xxxx` id appears in `leads.log`; confirm the email lands.
4. On the real host: confirm `Content-Encoding: br` or `gzip` on HTML, CSS and JS; open the site on an Android phone on mobile data and in the Instagram in-app browser.
5. **Lawyer (Paraguay)**: the places marked REVISAR ABOGADO in `docs/log/build.md` (neutral wording that replaced statute citations in privacy, terms and consent text; Ley 7593 commencement; processors named in the privacy page; character names Mickey, Minnie, Moana, Blanca Nieves, Frutillita).
6. Check yourself: the two hub paragraphs for Capiata and Mariano Roque Alonso (`zones.mjs`, REVISAR ANTON) and whether the Minnie image shows the number-one candle you want.
7. **Images that cost Higgsfield credits** (Sunburst model, needs Anton's yes): og.jpg with title text, heroes for `/revelacion-de-genero/` and `/primer-anito/`, card images for Kit Sorpresa and Revelacion Completa, five guide images, one retry each for Mickey and Blanca Nieves with prompts that describe only the listed elements and no character silhouette.
8. Local fonts (Satoshi and Instrument Serif woff2 files) or accept the Google Fonts fallback.
9. Be ready to answer WhatsApp within 24 business hours, and define what "24 horas habiles" means for a Saturday-evening lead.
10. Decide what to do with the two untracked `plan/prompts/DESIGN-FINAL-4.txt` and `-5.txt` (commit for provenance or delete).

## 6. Environment facts added this session

- `preview-server.mjs` now takes `PORT` env or the first argument. The session's `launch.json` is `C:\Claude 1\.claude\launch.json` (the parent folder), not the repo one; another window rewrote it and the babyshower entry is gone, so start the preview with `PORT=4185 node preview-server.mjs`.
- Codex `resume` needs the tier flags (the wrapper script handles that); prompts must contain no double quotes and should be ASCII: for accented copy point Codex at the file that contains it (the design boards and `docs/design-mix-home/gen.mjs`) and tell it to copy verbatim.
- Codex could not open a browser or repeat the preview at the end of a turn (its sandbox denied it): the manager does the visual audit.
- QA helpers in `docs/qa-tools/`: `shots.mjs` (sliced screenshots), `tap-targets.mjs` (undersized targets on all routes at 375 px, expects a server on 4185), `imgdiff.mjs` (pixel diff of two screenshot sets), `fcp-throttled.mjs` (real Chrome FCP under 4x CPU).
- `css-audit.mjs` (repo root) lists CSS rules that match no element on any built page; `.mjs` files never ship in the zip.
- Never use port 4190 for Node tooling. Do not stop a server on 4173 (another window may use it).

## 7. Next-session material

`docs/next-session/continue-prompt.md` (paste into a new Sonnet window), `docs/next-session/fable-review-prompt.md` (paste into a Fable 5.1 window Anton opens himself), `docs/next-session/codex-review-prompt.txt` (read-only Codex review, command inside the Fable file and in the chat).
