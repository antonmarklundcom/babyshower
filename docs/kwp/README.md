# Keyword Planner data (Paraguay, Spanish)

- `2026-09-kwp-paraguay.csv`: Anton's export of 2026-09-24. Rows with volume >= 20 plus the seeds;
  hundreds of 10/0-volume suggestions were left out. CPC was "—" (no bids) for every row, so
  cpc_low/cpc_high are empty. Competition is Google's Ads competition (Low/Medium/High).
- The older table (mostly generic décor terms, some CPC in SEK) is in `../../BRIEF.md` section 5.
- Add each new export as a new dated CSV with the same columns.
