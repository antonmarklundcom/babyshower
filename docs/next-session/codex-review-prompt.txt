You are a read-only reviewer for the repo babyshower.com.py (static site generated by build-site.mjs from content.mjs, themes.mjs and zones.mjs; CSS in assets/css/site.css; JS in assets/js; lead form handler lead-forward.php). Do not modify, create or delete any file. Do not run the build. Do not print or echo secrets. Read AGENTS.md, plan/02-BUILD-SPEC.md section 0 and docs/HANDOFF-2026-09-20-b.md first.

Review these areas and report only real, checked findings with file and line, separated into CONFIRMED (you traced it in code or built HTML) and SUSPECTED:
1. lead-forward.php: input validation and normalisation of the phone (spaces, dashes, plus sign, country code 595), the server-generated BS id when the sid field is empty, spam handling, rate limiting, header injection, log writing, email sending and failure paths, what happens when the private config file is missing.
2. assets/js/calc.js against the price rules in content.mjs (PACKAGES, ADDONS, ZONES, EXTRA_FOOD, PRICES): guest counts at the limits (15, 30, 40, 45, 60), bundled add-ons per package, zone Otra, zero-price captions, rounding and thousands separators, and the WhatsApp text it builds. Check that no personal data can leak into analytics parameters.
3. build-site.mjs and verify.mjs: any check that can pass while the output is wrong, any route or sitemap or canonical inconsistency, any page with a missing or duplicated title, description, h1 or og tag, broken internal links or anchors in the built HTML.
4. Accessibility in the built HTML of the home page, /combos-y-precios/, /contacto/ and one theme page: heading order, labels for every input, focus order, aria on the details elements and the mobile menu, tap target sizes in assets/css/site.css, contrast of the tint colours against text (compute ratios for the actual hex values used).
5. Section 0 compliance: search every built HTML file for the words Reservar, sena, pago online, garantizado, mejor, el mas, 100 por ciento (search both with and without accents), and for any price shown as firm instead of estimated; list every occurrence.
6. Dead code and leftovers: unused CSS selectors (css-audit.mjs exists), unused JS, files in the deploy zip that should not ship (see deploy/make-zip.ps1).

Output: a numbered list ordered by severity (blocker, should-fix, minor), each item with file, line, one-sentence problem, and a one-sentence fix that does not change section 0 wording. End with one line: overall verdict for publishing the site on a hosted server for inquiry-only use. Stay under 900 words.
