Options -Indexes
RewriteEngine On
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} !^babyshower\.com\.py$ [NC]
RewriteRule ^ https://babyshower.com.py%{REQUEST_URI} [R=301,L,NE]

# Hostinger Git deploy clones the whole repo here; only the built site is public.
RewriteCond %{REQUEST_URI} !^/\.well-known/
RewriteRule (^|/)\. - [F,L]
RewriteRule ^(plan|docs|codex-input|deploy|dist|node_modules)(/|$) - [F,L,NC]
RewriteRule \.(md|mjs|ps1)$ - [F,L,NC]
ErrorDocument 403 /404.html

<FilesMatch "(?i)(^leads\.log$|\.mjs$|^vendercrm-config.*\.php$)">
  Require all denied
</FilesMatch>

ErrorDocument 404 /404.html
AddDefaultCharset UTF-8

<IfModule mod_mime.c>
  AddType image/avif .avif
  AddType image/webp .webp
  AddType font/woff2 .woff2
</IfModule>

<IfModule mod_headers.c>
  # Asset URLs are versioned; HTML must re-fetch current prices.
  Header set Cache-Control "public, max-age=31536000, immutable" "expr=%{REQUEST_URI} =~ m#^/assets/#"
  <FilesMatch "(?i)\.html$">
    Header set Cache-Control "no-store"
  </FilesMatch>
</IfModule>

<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/css text/plain text/xml application/javascript text/javascript application/json application/xml application/ld+json image/svg+xml
  <IfModule mod_headers.c>
    Header append Vary Accept-Encoding
  </IfModule>
</IfModule>

<IfModule mod_brotli.c>
  AddOutputFilterByType BROTLI_COMPRESS text/html text/css text/plain text/xml application/javascript text/javascript application/json application/xml application/ld+json image/svg+xml
  <IfModule mod_headers.c>
    Header append Vary Accept-Encoding
  </IfModule>
</IfModule>
