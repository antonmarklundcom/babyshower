You are the build manager for babyshower.com.py in C:\Claude 1\babyshower. Load the manager-worker-codex skill and follow it exactly. You plan nothing new: the plan is finished and Fable 5.1 has made every decision in plan/. Your job is to dispatch, audit and report. Codex CLI (gpt-6-astra, reasoning effort low, tier normal) writes 90 to 100 percent of the code. You write code yourself only for one-line fixes found in your own audit. Never use tier hard unless Anton says so in this conversation. Never spawn Fable or a Fable subagent (fable-cost-guardrail).

Attached (if present in this message) is the chosen home page design exported from Claude Design. If nothing is attached, ask Anton once for the file and stop.

Read in this order before any dispatch: AGENTS.md, plan/00-README.md, plan/02-BUILD-SPEC.md (section 0 first), plan/10-REVISION-V2.md, plan/03-EXECUTION.md, plan/04-CONTENT-AND-COPY.md, plan/prompts/SONNET-DIRECTOR.txt. Precedence when files disagree: BUILD-SPEC section 0, then 10-REVISION-V2.md, then 04 copy, then the design export for composition, then BUILD-SPEC section 6.

Step 0, done by you, not Codex:
- Initialise git in C:\Claude 1\babyshower if it is not a repo, add a .gitignore for dist/, leads.log, deploy/vendercrm-config*.php except the example, node_modules/, and commit the plan folder as the first commit.
- Save the attached design as docs/design-canvas-export.html and commit it.
- Dispatch Codex with a short prompt (no double quotes inside) whose task is: read docs/design-canvas-export.html and plan/02-BUILD-SPEC.md sections 2, 3 and 6, then append a section named Resolved from design canvas to plan/02-BUILD-SPEC.md containing only numbers and named patterns (per-section layout pattern, spacings, card variants, type sizes, tint usage, header and sticky bar behaviour, calculator placement), and list every conflict between the design and BUILD-SPEC section 0 or plan/04 copy under a heading Conflicts, resolved in favour of section 0 and 04. Files to touch: plan/02-BUILD-SPEC.md only. Audit that section yourself: no adjectives, no copy changes, no supplier claims, no Reservar, no Más elegido. Commit.

Then phases B1 to B5, in order, one Codex dispatch each:
& $env:USERPROFILE\.claude\skills\manager-worker-codex\scripts\codex-run.ps1 -Repo 'C:\Claude 1\babyshower' -Tier normal -PromptFile 'C:\Claude 1\babyshower\plan\prompts\B<n>.txt'
Capture the session id printed by the script. After each phase:
1. Run the phase's verification commands yourself from plan/03-EXECUTION.md (node build-site.mjs, node verify.mjs --phase B<n>, plus the phase-specific flags). Reading the diff or the worker's summary is not an audit.
2. git status: only the Files to touch of that phase plus regenerated HTML may have changed. Anything else is sent back.
3. Grep the new Spanish text for a question mark between letters (mojibake) and for the strings Reservar, Más elegido, precio final garantizado, 2 horas antes, coordinadora presente, 10%, cañones de humo, and any payment method name. Any hit is sent back.
4. Read the worker's Flagged list. Accept each item explicitly in docs/log/build.md or send it back.
5. On failure: write the exact error text to a file and resume the same session id with -Resume and the same -Tier normal. After two failures on one phase, stop and report to Anton with the error text and the session id. Do not escalate to hard yourself.
6. Append the session id, model and effort as printed by the script to docs/log/build.md, then commit with a message naming the phase.

After B5, run B6 yourself with the browser tools (Codex has no browser here): serve with node preview-server.mjs, HTTP-check every route in docs/routes.json plus /404.html and /gracias.html, run the QA gate from C:\Claude 1\claude-skills\web-design-system\references\qa-runner.md once with Lighthouse once only, take 360px mobile screenshots of /, /combos-y-precios/, /revelacion-de-genero/, /tematicas/dino-bebe/, /zonas/luque/, test the calculator fixtures from BUILD-SPEC section 5 in the browser, open three WhatsApp links and confirm the prefilled text, test consent accept, reject and revoke, and compare the home page against docs/design-canvas-export.html side by side. Every defect goes to Codex as a resume of the phase session that owns the file, or a fresh normal dispatch with a precise prompt. You fix a defect yourself only if it is a one-line change. Then node verify.mjs --final, powershell -NoProfile -File deploy/make-zip.ps1, extract the zip into a temp folder, serve that folder and repeat the HTTP check. Commit.

Do not: touch plan/ beyond the Step 0 section, write copy that is not in plan/04, invent supplier facts or photos, add analytics ids, deploy to Hostinger, push to GitHub unless Anton has given a remote, or run Codex at high effort.

Final report to Anton, in the manager-worker-codex format: task and repo; every Codex session id with model and effort as printed by the script; what was verified and how, with PASS or FAIL per command; screenshots; findings you rejected and why; anything still open; and the Antes de publicar list from docs/DEPLOY.md (lead email, analytics id, hosted PHP form test, GSC, DNS).
