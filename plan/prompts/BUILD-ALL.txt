Read plan/02-BUILD-SPEC.md section 0 first, then plan/10-REVISION-V2.md (revision authority), before the other listed reading. Precedence: section 0 > 04 copy > design export (composition) > BUILD-SPEC section 6 section list. Once chosen, docs/design-canvas-export.html governs home composition in modes A/B/C; inner pages reuse its components. Until chosen, B1 uses section 6 patterns.

You may regenerate every output HTML file the generator produces. Authored source edits are limited to the files listed. Counts, sitemap and SHIP list derive from docs/routes.json: route, output, phase (B1..B4), indexable, built. Snapshot: 32 routes, 31 non-root route directories inside 11 top-level route directories, 34 HTML files including 404.html and gracias.html. Cumulative built routes B1/B2/B3/B4 = 6/9/26/32; never hardcode totals in implementations. verify.mjs --phase B1..B5 reads built flags; links to built:false are allowed only when the target exists in the manifest. --final rejects any built:false.

Each phase appends its own docs/log/build.md entry; manager or Anton adds session id/model/effort from dispatch output, never invented. PHP lint is not available locally: record php -l lead-forward.php as not available locally, not as a pass. Anton owns the Gate A hosted PHP test: submit one real form on the addon domain, check email + log. Deployment uses powershell -NoProfile -File deploy/make-zip.ps1 (.NET System.IO.Compression.ZipFile); the shell script is optional.

Task: Build the complete babyshower.com.py static site in one session, phase by phase. Read AGENTS.md, plan/02-BUILD-SPEC.md in full, plan/04-CONTENT-AND-COPY.md in full, then the phase prompts plan/prompts/B1.txt, B2.txt, B3.txt, B4.txt, B5.txt. Execute them in order. Each phase has its own Files to touch, Definition of done and Commands to run. Run every listed command at the end of each phase and stop the phase to fix before moving on if any command fails. Do not proceed to the next phase with a failing verify.

Rules that apply across all phases:
- Static HTML generated by node build-site.mjs. No npm packages, no framework, no GitHub Actions, no Vercel.
- All prices, texts, policies, WA_NUMBER, LAUNCH_MODE, BOOKING_ENABLED and PRICES live in content.mjs. Never hardcode a price in JS or HTML.
- Paraguayan Spanish, voseo in every CTA, lang es-PY, no English in rendered HTML.
- Never invent reviews, event counts, years in business, RUC, or licensed character claims.
- Never present generated images as real events. Placeholder panels are tinted blocks, never empty boxes.
- Do not touch plan/, codex-input/, BRIEF.md, AGENTS.md, or any file outside this repo.
- Never print or write secrets, tokens, or API keys.
- Do not drop, narrow, or substitute a step. If something is impossible, do everything else and flag it.

Definition of done for the whole run:
- node build-site.mjs, node verify.mjs --final, node verify.mjs --phase B2 --calc, node verify.mjs --phase B3 --uniqueness, node verify.mjs --phase B4 --words, powershell -NoProfile -File deploy/make-zip.ps1 all exit 0 from C:\Claude 1\babyshower.
- dist/babyshower-YYYY-MM-DD.zip contains the manifest-derived index.html files and the shipping files listed in B5.
- docs/log/build.md has one entry per phase.

Report format:
  Per phase: Files changed with one-line summaries, Commands run with PASS or FAIL
  Flagged or not done: every item across all phases, with the phase name, or None
  Antes de publicar: the list of values Anton must set before deploy
