You are running a local Claude Code session in C:\Claude 1\babyshower with the Higgsfield MCP connected. Task: generate the website images for babyshower.com.py with Higgsfield, download them, convert them, and wire them into the HTML. Load these skills first, in this order: higgsfield-image-pipeline, higgsfield-web-imagery, webimg-pipeline. Then read plan/07-IMAGERY.md (slots, prompts, base style block, negative block) and plan/02-BUILD-SPEC.md section 11 (slot names in the markup). Never spawn Fable or a Fable subagent.

Model decision for this project (Anton, 2026-09-19): this overrides Rule 1 of the higgsfield-image-pipeline skill, which mandates nano_banana_pro. Use model gpt_image_2_5 with variant sunburst and quality medium for every image. Resolution 1k by default; 2k only for the three hero-bleed slots and the section-break band. Pass model, variant, quality and resolution explicitly in every generate_image or generate_image_batch call; never let a default choose, never pass use_unlim, never switch model on your own. Verify by the credit ledger after each batch (transactions), record the display name and credits per job in the manifest, and stop if the ledger names a different model.

Inputs and precedence: the slot list and prompts in plan/07-IMAGERY.md are authoritative. If the attached or committed design export (docs/design-canvas-export.html) or the built pages define a slot 07 does not have, add it to the manifest with a prompt in the same style; do not drop a slot. If site HTML does not exist yet, stop after generating, downloading and converting into assets/img and writing the manifest, and report; do not build pages.

Procedure:
1. Rule 0 preflight from the skill: check docs/imagery-manifest.json, git log, and assets/img for anything already generated. Never regenerate a planned image that already exists or has a result URL in the manifest.
2. Write docs/imagery-manifest.json before generating: one entry per slot with id, file name (Spanish kebab-case from 07), alt text in Spanish, ratio, target px, model gpt_image_2_5, variant sunburst, quality medium, resolution, and the full prompt (base block + slot prompt + negative block). Total 30 images per 07.
3. Cost preflight: generate_image with get_cost true for one 1k and one 2k example; write the numbers into _notes.cost_preflight. Report the total expected credits to Anton and wait for a yes before spending.
4. Generate in batches by slot type with generate_image_batch, then jobs_wait, then one show_generation_by_ids per batch. Check every result has the expected ratio and pixel class. Write url, width, height and job_id into the manifest.
5. Download and convert locally with the webimg pipeline (this is a local machine, the CDN is reachable): WebP, hero at 1920 wide and under 180 KB, cards at 1200x900, og at 1200x630 jpg, filenames and alt text taken from the manifest, never typed by hand. Place under assets/img.
6. Wire: replace each tinted placeholder panel with a picture element or img using the manifest file and alt, width and height attributes set, loading lazy below the fold and eager on the hero, caption Imagen ilustrativa kept on every AI image, and add the og:image tag now that assets/img/og.jpg exists. Do not touch proof-photo slots; they stay hidden. Do not change any copy.
7. Rebuild and verify: node build-site.mjs, node verify.mjs --final. Serve with node preview-server.mjs and take one 360px screenshot of the home page and one of a theme page. Fix any broken path yourself.
8. Update _notes in the manifest: generated date, actual_spend_credits, ledger_checked, download_status done. Commit the manifest, the assets and the HTML in one commit.

Hard rules: no faces, no children, no text inside images, no recognisable characters (Moana, Minnie, Mickey, Blanca Nieves, Frutillita, Jefe en Pañales are shown by palette and generic motifs only), no smoke cannons in the reveal kit image, every AI image labelled Imagen ilustrativa, nothing presented as a real event. Never print or write API keys or tokens.

Report: expected versus actual credits, the manifest path, any slot that failed or was skipped with the reason, and the two screenshots.
